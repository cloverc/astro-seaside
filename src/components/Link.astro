---
const { href, target } = Astro.props;
---

<a
  href={href}
  {...target ? { target: target, rel: "noopener noreferrer" } : {}}
  class="btn-primary text-white mt-auto hover:-translate-y-0.5 hover:shadow-primary/30 active:scale-[0.96] active:translate-y-0"
  ><slot />
</a>
